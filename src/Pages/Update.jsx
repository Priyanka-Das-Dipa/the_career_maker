
import {  useLoaderData } from "react-router-dom";

const Update = () => {
  const product = useLoaderData()
    const {_id, }
  

  return (
    <div>
      this is update
    </div>
  );
};

export default Update;
